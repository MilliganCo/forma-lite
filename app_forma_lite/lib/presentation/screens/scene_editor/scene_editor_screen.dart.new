import 'package:flutter/material.dart';

class SceneEditorScreen extends StatelessWidget {
  const SceneEditorScreen({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('Scene Editor')),
      );
}
